# Placeholder articles

All posts in this directory are **placeholder scaffolding** written to exercise
the Journal layout — headings, blockquotes, lists, tables. Replace them with
real editorial content before launch.

Frontmatter must match the `posts` schema in `src/content.config.ts`.

This README is excluded by the loader pattern (`!**/README.md`).
